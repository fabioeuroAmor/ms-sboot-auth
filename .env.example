# ==============================================================================
# ms-sboot-auth — variaveis de ambiente por profile (dev / hom / prd)
# Copie para .env e preencha com valores reais. NUNCA commitar o .env real.
# ==============================================================================

# ------------------------------------------------------------------------------
# DEV — hoje sao valores fixos em application-dev.yaml, nao ${VAR}.
# Sobrescrevem o yaml se definidos, usando os nomes padrao de relaxed binding.
# ------------------------------------------------------------------------------
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/postgres?stringtype=unspecified
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=postgres
SPRING_DATA_REDIS_HOST=localhost
SPRING_DATA_REDIS_PORT=6379
JWT_SECRET=sgsm-chave-desenvolvimento-local-minimo-256-bits-nao-usar-em-producao
# SMTP em dev usa um remetente de teste (MailerSend sandbox) - senha vazia por padrao.
SPRING_MAIL_USERNAME=MS_3umjcb@test-65qngkdq773lwr12.mlsender.net
SPRING_MAIL_PASSWORD=
TRACING_SAMPLING_PROBABILITY=1.0
ZIPKIN_ENDPOINT=http://localhost:9411/api/v2/spans

# ------------------------------------------------------------------------------
# HOM — application-hom.yaml ja usa ${VAR} em todos os campos abaixo.
# ------------------------------------------------------------------------------
# DATASOURCE_URL=
# DATASOURCE_USERNAME=
# DATASOURCE_PASSWORD=
# REDIS_HOST=
# REDIS_PORT=6379
# SMTP_HOST=smtp.mailersend.net
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=
# JWT_SECRET=
# TRACING_SAMPLING_PROBABILITY=1.0
# ZIPKIN_ENDPOINT=

# ------------------------------------------------------------------------------
# PRD — mesmas variaveis de HOM (nomes identicos), valores de producao.
# Gerar segredos NOVOS - nunca reaproveitar valor de dev/hom.
# ------------------------------------------------------------------------------
# DATASOURCE_URL=
# DATASOURCE_USERNAME=
# DATASOURCE_PASSWORD=
# REDIS_HOST=
# REDIS_PORT=6379
# SMTP_HOST=smtp.mailersend.net
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=
# JWT_SECRET=
# TRACING_SAMPLING_PROBABILITY=0.1
# ZIPKIN_ENDPOINT=
